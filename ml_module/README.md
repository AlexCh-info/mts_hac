# Выше крыши - ML часть
# Суть проекта
Создание модели для предсказания пропусков высот в финальном датасете (merge_dataset.csv)

# Структура проекта
```
ml_module/
-/data
--/report.txt
-/src
--/feature_selection_rfecv.py
--/spatial_split.py
--/train_lgb.py
--/predict.py
--/report.txt
```

# Термины и сокращения
  1.  Предметная область, далее в тексте ПО.
  2.  Машинное обучения, далее в тексте МО.
  3.  Target - целевая переменная.
  4.  Бустинг - алгоритм МО (LightGBM, XGBoost, CatBoost), в основе лежат деревья решений, каждое новое дерево исправляет ошибки предыдущего.
  5.  Веса - в контексте МО, степень важности, чем выше вес, тем важнее признак.
  6.  Кросс Валидация - Техника при которой, данные деляться на части например на 4, 3 из этих частей попадают в тренировочную выборку, 1 часть в валидационную, в ходе обучения каждая часть данных попадает в валидационную выборку.
  7.  Фолд (Fold) - часть данных при кросс валидации.

# Ход работы
## Изучение задачи
Первым делом стоит задача изучения ПО - это делается для того, чтобы понять суть задачи, данных и определить то, что мы хотим получить как конечный результат.
Как вы знаете из общего README и README части с обработкой датасета наша задача заключается в заполнении пропусков с помощью алгоритма МО. Однако, как профессионал
мы в первую очередь должны понять, реально ли нам нужен алгоритм МО для выполнения данной задачи. 

Тут мы приходим к особенности человеческого организма, а точнее к способности анализировать большие массивы информации. Дело в том, что человек способен 
обрабатывать до 1000 единиц информации (будь то строки, числа и т.д.) без каких-либо проблем, опечаток, пропусков. Но после этого порога человек начинает 
путаться, пропускать информацию и ошибаться. Поэтому ручная разметка сразу отпадает, как решение нашей задачи т.к. в нашем распоряжении примерно 170тыс. строк информации.

Переходим к следующему шагу. На этом шаге мы должны убедиться, что нет простого алгоритма (не МО), который помог бы нам решить эту задачу. Однако, посмотрев множество источников
информации по теме "Геоаналитика" (т.к. я впервые работаю с такой задачей), я пришел к выводу, что в нашем случае, я потеряю большое кол-во времени на поиск того, как реализовать этот простой алгоритм. Так что переходим к сл. шагу.

# Анализ признаков в датасете, удаление утечки данных и т.д.
Этот шаг я вынес в отдельный раздел т.к. на нем возникло множество проблем. 

# Первые ошибки (то что не увидете, но должны об этом знать)
В этом разделе я расскажу о том, что не вошло в финальную версию программы.

Начнем из далека, прежде всего нельзя доверять данным на сто процентов, поэтому первым делом надо удалить признаки, которые имеют сильную корреляцию с target-ом. Для этого в первой версии программы я написал скрипт, который оценивал множество признаков, такие как корреляция, RFE и выделение признаков с помощью алгоритма МО (RandomForestRegressor и LightGBM), этот скрипт был относительно хорош и помог выделить множество признаков, из 61 в начальном датасете, осталось 48. 

## Выбор модели 
Переходим дальше, я не стал дальше отбирать признаки, полностью доверевшись скрипту (что и насолило мне потом), далее я приступил к выбору модели-моделей МО для реализации основной задачи - предсказанию высот зданий. 

На этом моменте было много споров с командой, что и как использовать. Бондаренко Григорий говорил, что мы можем использовать регрессию, я говорил, что предлагал бустинги, Шкодин Роман предложил ансамбль моделей. В ходе долгих рассуждений и моих объяснений, мы пришли к выводу, что лучше всего использовать ансамбль бустингов для решения нашей задачи. 

## Почему бустинги 
Бустинги благодаря своей особенности могут извлекать сложные нелинейнные зависимости, а в контексте наших данных это большое преимущество. Думаю вопросы о том, почему мы не выбрали линейную регрессию должны пропасть. Да задача очень похожа на регрессию (она это и есть), но обычные модели регрессии (на сколько бы они сильными не были) не могу извлекать нелинейнные сложные зависимости + требуется серьезная подготовка данных (пропуски, значения, которые регрессия не может обработать, категориальные признаки).

## Выбор моделей для ансамбля
Выбор пал на довольно базовые модели такие как lightGBM, XGBoost, CatBoost, RandomForestRegressor. Суть метода заключалась в том, что мы обучем каждую модель по отдельности на данных, смотрим на метрики качества и моделям с лучшим качеством даем больший вес, затем общий результат предсказаний делим с учетом весов и получаем предсказание высоты.

## Метрики качества 
В качестве метрик были выбраны следующие:
  1.  RMSE
  2.  MAE
  3.  R2
Почему такой набор?
RMSE - Сильнее штрафует за ошибки при обучении, однако, Результат выдается в тех же единицах, что и целевой признак (например, в рублях или метрах), что делает его понятным для анализа.

MAE - Менее чуствительна к высборсам (если они есть), она может быть интерпретирована как устойчивость модели.

R2 - позволяет быстро понять общее качество модели в диапазоне от 0 до 1, не привязываясь к масштабу цен или других величин. Значение 0.85 означает, что модель объясняет 85% изменчивости данных.

Эти метрики мы будем использовать и в нынешней версии программы.

## Обучение

На этом этапе возникли основные проблемы, которые и побудили меня изменить подход к обучения и вернуться назад к выбору признаков и частично заняться feature-engineeringом заново.
Для наглядного примера давайте посмотрим на график того, на сколько большой вклад внесли модели в предсказания:
<img width="1198" height="649" alt="image" src="https://github.com/user-attachments/assets/7b0ad8c8-9050-4afb-8f06-83f08d627c73" />

На графике видно, что первые две модели (LightGBM, XGBoost) вообще не получили весов, а основное распределение пришлось на CatBoost и RandomForestRegressor. Но почему же так, как оказалось позже предсказания были сликом точные, например, ошибка MAE у первых двух моделей составила infinitive (слишком малое здачение, ниже чем 1e-6), у двех последних же моделей MAE составила 0.08 в коонтексте нашей ситуации это примерно 8см, на этом моменте можно было обрадоваться и закончить разработку моделей. Однако, для меня это стало тревожным звонком. Я попробовал KFold подход с Cross Validation Metrics, в нашем случае за CV отвечали MAE и R2. Посмотрим на график:
<img width="2972" height="1767" alt="learning_curves" src="https://github.com/user-attachments/assets/0457a18c-7b4c-4a91-85d2-c5136788a6cc" />

Что можно сказать из четырех моделей ансамбля видно только две а значение MAE очень низкое, это еще раз подтверждает теорию о том, что с данными есть проблемы, осталось выяснить, что это за проблема.

## Утечка данных
В качестве проверки гипотезы об утечке данных, я выбрал независимую модель HistGradientBoostRegressor из библиотеки Sklearn.
Как показало обучение метрика R2 была равно 0.999 на тренировочных данных и 0.998 на валидационных данных, при этом модель плохо себя вела на тестовых данных, то же самое происходило и с метрикой MAE, которая была низкой на тренировочных и валидационных данных, но на тестовых зашкаливала.

Это исследование подтвердило  мое опасение по поводу того, что в данных присутсвует сильная утечка данных, которую мы не смогли выявить при первой проверке признаков.

# Нынешнее решение
## Анализ признаков
Как вы уже знаете мы столкнулись с утечкой данных, нужно было использовать совершенно другие подходы по выявлению признаков влияющих на обучение в негативном ключе. Это признаки, которые помогают косвенно модели узнать высоту, при этом все внимание модели они забирают на себя. Чтобы обнаружить такие признаки, был использован метод RFECV.

RFECV - это тот же RFE, но более простой в использовании, он помогает подобрать необходимое кол-во признаков, найти признаки, которые уводят внимание модели на себя и делает это все автоматически, используя логику KFold (Cross Validation)

Посмотрим на результаты запуска:
<img width="1045" height="1265" alt="image" src="https://github.com/user-attachments/assets/33bb0537-1acb-43e9-b40f-8472eb4307d5" />

Рядом с признаками я пометил те, которые нужно убрать, большинство из них это те, при вычислении которых использовалась target переменная или признаки похожие на target, а так же мусор.

Но этого анализа все равно не достаточно, поэтому при обучении модели я выводил графики важности признаков, чтобы следить за качеством модели.

# Обучение (LightGBM)
Как тестовая и финальная модели была взята за основу LightGBM, у нее много полезных функций и она проста в использовании, а так же достаточно сильна, чтобы находить нелинейные зависимости. Был вариант использовать CatBoost, если LightGBM упрется в потолок.

В файле spatial_split.py представлен первый прототип модели с использованием Spatial-Split.

Spatial-Split - это модификация разделения данных на выборки, так как у нас была утечка данных и данные представляют из себя участки, то могла случиться ситуация, что модель обучалась бы на данных с участков находившихся рядом друг с другом и брать данные для предсказания с прошлых данных. Так что мы разделили данные на группы по h3 индексу и перемешивали данные так, чтобы участки стоящие рядом, не шли друг за другом при обучении.

Для обучения модели так же была применина кросс валидация, что улучшало обучение модели.

Данные были разделены на 7 фолдов.
Первая конфигурация модели выглядила так:

```
model = lgb.LGBMRegressor(
        n_estimators=1000,
        learning_rate=0.05,
        max_depth=-1,
        num_leaves=64,
        min_child_samples=12,
        subsample=0.8,
        colsample_bytree=0.8,
        random_state=42
    )

    model.fit(
        X_train, y_train,
        eval_set=[(X_val, y_val)],
        eval_metric="rmse",
        callbacks=[
            lgb.early_stopping(100),
            lgb.log_evaluation(100)
        ]
    )
```

Ее средний rmse = 17.9 м 
То есть, при предсказании модель могла предсказать высоту здания выше либо ниже на 17-18м, это неудовлетворительный результат, ошибка слишком велика.

## 1 этап fine tuning
На первом этапе я с опаской менял конфигурацию модели, у меня в голове была допустимая ошибка (это 3м), результат до которого надо дойти.
Вторая концигурация модели выглядила вот так:
```
model = lgb.LGBMRegressor(
        n_estimators=2000,
        learning_rate=0.003,
        max_depth=-1,
        num_leaves=128,
        min_child_samples=10,
        subsample=0.9,
        colsample_bytree=0.9,
        random_state=42
    )

    model.fit(
        X_train, y_train,
        eval_set=[(X_val, y_val)],
        eval_metric="rmse",
        callbacks=[
            lgb.early_stopping(100),
            lgb.log_evaluation(100)
        ]
    )
```

Мы повысили кол-во эпох, снизили скорость обучения и изменили чуть чуть другие параметры такие как num_leaves, min_child_samples и т.д.

Ее средний rmse = 16.2м
Уже лучше, но это все равно много.


## 2 этап fine tuning
На втором этапе fine tuning я начал поднимать параметры 
Вторая концигурация модели выглядила вот так:
```
model = lgb.LGBMRegressor(
        n_estimators=2000,
        learning_rate=0.003,
        max_depth=-1,
        num_leaves=128,
        min_child_samples=10,
        subsample=0.9,
        colsample_bytree=0.9,
        random_state=42
    )

    model.fit(
        X_train, y_train,
        eval_set=[(X_val, y_val)],
        eval_metric="rmse",
        callbacks=[
            lgb.early_stopping(100),
            lgb.log_evaluation(100)
        ]
    )
```

Мы повысили кол-во эпох, снизили скорость обучения и изменили чуть чуть другие параметры такие как num_leaves, min_child_samples и т.д.

Ее средний rmse = 16.2м
Уже лучше, но это все равно много.

## 3 этап fine tuning
На первом этапе я с опаской менял конфигурацию модели, у меня в голове была допустимая ошибка (это 3м), результат до которого надо дойти.
Вторая концигурация модели выглядила вот так:
```
model = lgb.LGBMRegressor(
        n_estimators=2000,
        learning_rate=0.003,
        max_depth=-1,
        num_leaves=128,
        min_child_samples=10,
        subsample=0.9,
        colsample_bytree=0.9,
        random_state=42
    )

    model.fit(
        X_train, y_train,
        eval_set=[(X_val, y_val)],
        eval_metric="rmse",
        callbacks=[
            lgb.early_stopping(100),
            lgb.log_evaluation(100)
        ]
    )
```

Мы повысили кол-во эпох, снизили скорость обучения и изменили чуть чуть другие параметры такие как num_leaves, min_child_samples и т.д.

Ее средний rmse = 16.2м
Уже лучше, но это все равно много.

## 4 этап fine tuning
На первом этапе я с опаской менял конфигурацию модели, у меня в голове была допустимая ошибка (это 3м), результат до которого надо дойти.
Вторая концигурация модели выглядила вот так:
```
model = lgb.LGBMRegressor(
        n_estimators=2000,
        learning_rate=0.003,
        max_depth=-1,
        num_leaves=128,
        min_child_samples=10,
        subsample=0.9,
        colsample_bytree=0.9,
        random_state=42
    )

    model.fit(
        X_train, y_train,
        eval_set=[(X_val, y_val)],
        eval_metric="rmse",
        callbacks=[
            lgb.early_stopping(100),
            lgb.log_evaluation(100)
        ]
    )
```

Мы повысили кол-во эпох, снизили скорость обучения и изменили чуть чуть другие параметры такие как num_leaves, min_child_samples и т.д.

Ее средний rmse = 16.2м
Уже лучше, но это все равно много.
