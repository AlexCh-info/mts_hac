# Выше крыши Модуль очистки и соединения датасетов
# Суть модуля
Подготовка датасетов к объединению, их объединение и подготовка данных для обучения.
# Структура проекта
```
data_cleaner_module/
-/config
--/config.py
-/src
--/init.py
--/data_cleaner.py
--/data_loader.py
--/data_merge.py
--/feature_engineering.py
--/main.py
--/data_quality_report.json
--/data_quality_analyzer.py
```
# Термины и сокращения
  -  Машинное обучение - далее в тексте МО.
  -  Предметная область - далее в тексте ПО.
  -  Spatial Join - пространственное соединение, основным в этом подходе явяется Геометрический анализ.


# Ход работы
## Основные задачи
  -  Изучение структуры датасетов.
  -  Разработать модуль обработки датасетов.
  -  Изучить методы соединения датасетов и соединить их вместе.
  -  Подготовить к передаче в модуль МО.
## Первый этап "Изучение данных".
Расмотрим данные предложенные в кейсе для его выполнения, они выглядят вот так:

### ИСТОЧНИК А
  -  Размер файла: 39 Мб.
  -  Количество записей: 171 453.

Поля:

  -  id — идентификатор полигона;
  -  title — наименование объекта;
  -  tags — теги (тип объекта);
  -  geometry — геометрия в формате WKT;
  -  area_sq_m — площадь полигона, м2;
  -  gkh_address — адрес из базы «Реформа ЖКХ»;
  -  gkh_floor_count_min — минимальная этажность по данным приложения «Реформа ЖКХ»;
  -  gkh_floor_count_max — максимальная этажность по данным приложения «Реформа ЖКХ».

### ИСТОЧНИК Б
  -  Размер файла: 82 Мб.
  -  Количество записей: 160 610.

Поля:

  -  subject — субъект РФ;
  -  district — муниципальный район;
  -  type — тип населенного пункта;
  -  locality — наименование населенного пункта;
  -  type_street — тип улицы;
  -  name_street — наименование улицы;
  -  number — номер дома;
  -  letter — литера;
  -  fraction — дробь;
  -  housing — корпус;
  -  building — строение;
  -  purpose_of_building — назначение здания;
  -  stairs — этажность;
  -  avg_floor_height — средняя высота этажа, м;
  -  height — высота здания, м;
  -  wkt — геометрия в формате WKT.

## Выводы по оценке датасетов
### Первый шаг
Взглянем на логи программы для оценки геометрии в обоих датасетах:
```
2026-03-24 18:50:14,250 - INFO - Запуск пайплайна: Единая модель высотности зданий (МТС хакатон)
2026-03-24 18:50:14,251 - INFO - Рабочая директория: D:\mts\src
2026-03-24 18:50:14,251 - INFO - Шаг 1: Загрузка и предобработка данных
2026-03-24 18:50:14,251 - INFO - Загрузка D:\mts\data\cup_it_example_src_A.csv
2026-03-24 18:50:14,940 - INFO - CSV загружен: 171454 строк, колонки: ['Unnamed: 0', 'id', 'title', 'tags', 'geometry', 'area_sq_m', 'gkh_address', 'gkh_floor_count_min', 'gkh_floor_count_max']
2026-03-24 18:50:14,940 - INFO - Используем колонку геометрии: 'geometry'
2026-03-24 18:50:14,940 - INFO - Парсинг WKT-геометрий...
2026-03-24 18:50:14,955 - INFO - Всего записей: 171453, не-NULL геометрий: 171454 (100.0%)
2026-03-24 18:50:21,312 - INFO - Валидных геометрий: 171453 (100.0%)
2026-03-24 18:50:21,374 - INFO - GeoDataFrame создан: 171453 объектов, CRS: EPSG:4326
2026-03-24 18:50:21,410 - INFO - Типы геометрий: {'Polygon': 171173, 'MultiPolygon': 281}
2026-03-24 18:50:21,416 - INFO - Загрузка D:\mts\data\cup_it_example_src_B.csv
2026-03-24 18:50:22,845 - INFO - CSV загружен: 161076 строк, колонки: ['Unnamed: 0', 'subject', 'district', 'type', 'locality', 'type_street', 'name_street', 'number', 'letter', 'fraction', 'housing', 'building', 'purpose_of_building', 'stairs', 'avg_floor_height', 'height', 'wkt', 'id']
2026-03-24 18:50:22,845 - INFO - Используем колонку геометрии: 'wkt'
2026-03-24 18:50:22,845 - INFO - Парсинг WKT-геометрий...
2026-03-24 18:50:22,863 - INFO - Всего записей: 161076, не-NULL геометрий: 161076 (100.0%)
2026-03-24 18:50:29,812 - INFO - Валидных геометрий: 161076 (100.0%)
2026-03-24 18:50:29,886 - INFO - GeoDataFrame создан: 161076 объектов, CRS: EPSG:4326
2026-03-24 18:50:29,920 - INFO - Типы геометрий: {'Polygon': 161069, 'MultiPolygon': 7}
2026-03-24 18:50:31,513 - INFO - Добавлены базовые признаки для источника A
2026-03-24 18:50:32,808 - INFO - Добавлены базовые признаки для источника B
2026-03-24 18:50:32,879 - INFO - Источник А: 171453 объектов
2026-03-24 18:50:32,879 - INFO - Источник Б: 161076 объектов
2026-03-24 18:50:32,879 - INFO - Шаг 2: Очистка и валидация геометрий
2026-03-24 18:50:32,879 - INFO - Очистка геометрий: 171453 записей
2026-03-24 18:52:07,882 - INFO - Очистка завершена: удалено 0 записей (0.0%), осталось 171454
2026-03-24 18:52:07,882 - INFO - Очистка геометрий: 161076 записей
2026-03-24 18:53:36,942 - INFO - Очистка завершена: удалено 0 записей (0.0%), осталось 161076
2026-03-24 18:53:36,943 - INFO - После очистки А: 171453 объектов (удалено 0)
2026-03-24 18:53:36,943 - INFO - После очистки Б: 161076 объектов (удалено 0)
```
По логам программы мы можем утверждать, что голонка с геометриией не повреждена в 100% случаев, это хорошо, потому что основная проблема данных этого датасета именно геометрические признаки т.к. они имеют специфический расширения.

Так же все геометрии не являются нулевыми, что хорошо.

### Второй шаг
Посмотрим на более усовершенствованный способ оценки для этого используем файл data_quality_analyzer.py (к сожалению при запуске main.py он не будет вызван автоматически т.к. в ходе работы код модуля main.py был неоднократно переписан, в конце этого раздела будет показано как запустить data_quality_analyzer.py)

Перейдем к логам:
```
2026-03-25 19:56:22,683 - __main__ - INFO - 
Шаг 1/8: Загрузка данных
2026-03-25 19:56:22,684 - src.data_loader - INFO - Загрузка D:\mts\data\cup_it_example_src_A.csv
2026-03-25 19:56:23,357 - src.data_loader - INFO - CSV загружен: 171454 строк
2026-03-25 19:56:23,357 - src.data_loader - INFO - Колонка геометрии: 'geometry'
2026-03-25 19:56:29,717 - src.data_loader - INFO - Валидных геометрий: 171454 (100.0%)
2026-03-25 19:56:29,782 - src.data_loader - INFO - GeoDataFrame: 171454 объектов, CRS: EPSG:4326
2026-03-25 19:56:29,788 - src.data_loader - INFO - Загрузка D:\mts\data\cup_it_example_src_B.csv
2026-03-25 19:56:31,187 - src.data_loader - INFO - CSV загружен: 161076 строк
2026-03-25 19:56:31,187 - src.data_loader - INFO - Колонка геометрии: 'wkt'
2026-03-25 19:56:38,248 - src.data_loader - INFO - Валидных геометрий: 161076 (100.0%)
2026-03-25 19:56:38,322 - src.data_loader - INFO - GeoDataFrame: 161076 объектов, CRS: EPSG:4326
2026-03-25 19:56:39,905 - src.data_loader - INFO - Добавлены признаки для источника A
2026-03-25 19:56:41,267 - src.data_loader - INFO - Добавлены признаки для источника B
2026-03-25 19:56:41,349 - __main__ - INFO - Источник А: 171,454
2026-03-25 19:56:41,349 - __main__ - INFO - Источник Б: 161,076
2026-03-25 19:56:41,349 - __main__ - INFO - 
Шаг 2/8: Анализ качества
2026-03-25 19:56:41,349 - src.data_quality_analyzer - INFO - Анализ качества источника: Source_A
2026-03-25 19:58:21,855 - src.data_quality_analyzer - INFO - Полнота: 44.3%
2026-03-25 19:58:21,855 - src.data_quality_analyzer - INFO - Топология: 100.0% валидных
2026-03-25 19:58:21,855 - src.data_quality_analyzer - INFO - Выбросы: 8350 объектов
2026-03-25 19:58:21,855 - src.data_quality_analyzer - INFO - Рекомендаций: 1
2026-03-25 19:58:21,855 - src.data_quality_analyzer - INFO - Анализ качества источника: Source_B
2026-03-25 19:59:55,927 - src.data_quality_analyzer - INFO - Полнота: 56.4%
2026-03-25 19:59:55,928 - src.data_quality_analyzer - INFO - Топология: 100.0% валидных
2026-03-25 19:59:55,928 - src.data_quality_analyzer - INFO - Выбросы: 2931 объектов
2026-03-25 19:59:55,928 - src.data_quality_analyzer - INFO - Рекомендаций: 1
2026-03-25 19:59:55,929 - src.data_quality_analyzer - INFO - Отчёт о качестве данных: D:\mts\reports\data_quality_report.json
2026-03-25 19:59:55,929 - __main__ - INFO - 
Шаг 3/8: Очистка
2026-03-25 19:59:55,929 - src.geometry_cleaner - INFO - Очистка: 171454 записей (порог: 10.0 м²)
2026-03-25 19:59:55,946 - src.geometry_cleaner - INFO - Классификация сараев/гаражей...
2026-03-25 20:00:00,226 - src.geometry_cleaner - INFO - Найдено: 7671 (4.5%)
2026-03-25 20:00:00,227 - src.geometry_cleaner - INFO - Очистка геометрий...
2026-03-25 20:01:35,727 - src.geometry_cleaner - INFO - Удалено: 1389 (0.8%), осталось: 170065
2026-03-25 20:01:35,728 - src.geometry_cleaner - INFO - Очистка: 161076 записей (порог: 10.0 м²)
2026-03-25 20:01:35,753 - src.geometry_cleaner - INFO - Классификация сараев/гаражей...
2026-03-25 20:01:39,852 - src.geometry_cleaner - INFO - Найдено: 106 (0.1%)
2026-03-25 20:01:39,852 - src.geometry_cleaner - INFO - Очистка геометрий...
2026-03-25 20:03:09,458 - src.geometry_cleaner - INFO - Удалено: 269 (0.2%), осталось: 160807
```
Основное внимание стоит уделить очистке данных, нахождению малых зданий, очистке геометрий и т.д.
Этот шаг важен т.к. он позволяет более подробно изучить качество и природу данных, весь анализ сохранен в json (data_quality.json)

#### Как запустить data_quality_analyzer.py
Вариант 1: Анализ после очистки (рекомендуется)
```
# В main.py — добавить после этапа очистки:

from src.data_quality import DataQualityAnalyzer

def main(args):
    # ... существующий код очистки ...
    
    #НОВЫЙ ЭТАП: Анализ качества
    logger.info("ШАГ X: Анализ качества очищенных данных")
    
    analyzer = DataQualityAnalyzer()
    
    # Анализируем оба источника
    report_a = analyzer.analyze_source(gdf_a_clean, "Source_A")
    report_b = analyzer.analyze_source(gdf_b_clean, "Source_B")
    
    # Экспорт сводного отчёта
    analyzer.export_report(config.RESULTS_DIR / 'quality_assessment.json')
    
    # Блокеров для продолжения пайплайна
    if report_a['topological_correctness']['validity_rate'] < 90:
        logger.warning("Низкое качество геометрии в Source А!")
    
    # ... продолжение пайплайна (слияние, признаки) ...
```
Вариант 2: Анализ сырых данных (для аудита источника)
```
# audit_raw_data.py — проверка качества до любой обработки
from src.data_loader import load_source
from src.data_quality import DataQualityAnalyzer
from config.config import config

analyzer = DataQualityAnalyzer()

# Загружаем "как есть", без очистки
gdf_raw = load_source(str(config.SOURCE_A), geometry_col='geometry')

# Анализируем
report = analyzer.analyze_source(gdf_raw, source_name="Source_A_RAW")

# Выводим "красные флаги"
print("\n Критические проблемы:")
for rec in report['recommendations']:
    if rec.startswith('!') or 'невалид' in rec.lower():
        print(f"{rec}")
```
Вариант-3: Запуск с команной строки (оболочка на файл)
```
#!/usr/bin/env python3
"""
CLI для анализа качества геоданных.
Пример: python analyze_quality.py --source A --export
"""

import argparse
import sys
from pathlib import Path
sys.path.append(str(Path(__file__).parent))

from config.config import config
from src.data_cleaner import DataCleaner
from src.data_quality import DataQualityAnalyzer

def main():
    parser = argparse.ArgumentParser(description='Анализ качества геоданных')
    parser.add_argument('--source', choices=['A', 'B', 'both'], default='both',
                        help='Какой источник анализировать')
    parser.add_argument('--raw', action='store_true', 
                        help='Анализировать сырые данные (без очистки)')
    parser.add_argument('--export', action='store_true',
                        help='Экспортировать отчёт в JSON')
    parser.add_argument('--strict', action='store_true',
                        help='Выйти с ошибкой при критических проблемах')
    
    args = parser.parse_args()
    
    analyzer = DataQualityAnalyzer()
    exit_code = 0
    
    sources = [('A', config.SOURCE_A, 'geometry'), ('B', config.SOURCE_B, 'wkt')]
    if args.source != 'both':
        sources = [s for s in sources if s[0] == args.source]
    
    for src_name, src_path, geom_col in sources:
        print(f"\nАнализ источника {src_name}: {src_path.name}")
        
        # Загрузка
        if args.raw:
            from src.data_loader import load_source
            gdf = load_source(str(src_path), geometry_col=geom_col)
        else:
            cleaner = DataCleaner(f"QualityCheck_{src_name}")
            gdf = cleaner.load_and_clean(str(src_path), geometry_col=geom_col)
        
        # Анализ
        report = analyzer.analyze_source(gdf, f"{src_name}_{'raw' if args.raw else 'clean'}")
        
        # Вывод рекомендаций
        if report['recommendations']:
            print("\nРекомендации:")
            for rec in report['recommendations']:
                print(f"{rec}")
        
        # Strict mode: проверка критических порогов
        if args.strict:
            if report['topological_correctness']['validity_rate'] < 85:
                print(f"Критически низкое качество геометрии!")
                exit_code = 1
            if report['completeness']['overall'] < 70:
                print(f"Недостаточная полнота данных!")
                exit_code = 1
    
    # Экспорт
    if args.export:
        path = analyzer.export_report(config.RESULTS_DIR / 'quality_report.json')
        print(f"\nОтчёт сохранён: {path}")
    
    sys.exit(exit_code)

if __name__ == '__main__':
    main()
```
Запуск
```
# Анализ обоих очищенных источников
python analyze_quality.py

# Только источник А, сырые данные
python analyze_quality.py --source A --raw

# С экспортом отчёта
python analyze_quality.py --export

# Строгий режим (выход с ошибкой при проблемах)
python analyze_quality.py --strict

# Комбинация: анализ Б + экспорт + строгий режим
python analyze_quality.py --source B --export --strict
```

### Третий шаг 
В data_cleaner.py реализована логика третьего шага.

Прежде чем соединять два датасета, надо их очистить привести все данные в нужный вид и т.д.

Посмотрим логи выолнения этого модуля:
```
Шаг 1/8: Загрузка данных
2026-03-26 13:06:16,817 - src.data_loader - INFO - Загрузка D:\mts\data\cup_it_example_src_A.csv
2026-03-26 13:06:17,511 - src.data_loader - INFO - CSV загружен: 171454 строк
2026-03-26 13:06:17,512 - src.data_loader - INFO - Колонка геометрии: 'geometry'
2026-03-26 13:06:23,818 - src.data_loader - INFO - Валидных геометрий: 171454 (100.0%)
2026-03-26 13:06:23,885 - src.data_loader - INFO - GeoDataFrame: 171454 объектов, CRS: EPSG:4326
2026-03-26 13:06:23,892 - src.data_loader - INFO - Загрузка D:\mts\data\cup_it_example_src_B.csv
2026-03-26 13:06:25,354 - src.data_loader - INFO - CSV загружен: 161076 строк
2026-03-26 13:06:25,354 - src.data_loader - INFO - Колонка геометрии: 'wkt'
2026-03-26 13:06:32,380 - src.data_loader - INFO - Валидных геометрий: 161076 (100.0%)
2026-03-26 13:06:32,457 - src.data_loader - INFO - GeoDataFrame: 161076 объектов, CRS: EPSG:4326
2026-03-26 13:06:34,102 - src.data_loader - INFO - Добавлены признаки для источника A
2026-03-26 13:06:35,385 - src.data_loader - INFO - Добавлены признаки для источника B
2026-03-26 13:06:35,460 - __main__ - INFO -  Источник А: 171,454
2026-03-26 13:06:35,460 - __main__ - INFO - Источник Б: 161,076
2026-03-26 13:06:35,461 - __main__ - INFO - 
Шаг 2/8: Анализ качества
2026-03-26 13:06:35,461 - src.data_quality_analyzer - INFO - Анализ качества источника: Source_A
2026-03-26 13:08:16,474 - src.data_quality_analyzer - INFO - Полнота: 44.3%
2026-03-26 13:08:16,474 - src.data_quality_analyzer - INFO - Топология: 100.0% валидных
2026-03-26 13:08:16,474 - src.data_quality_analyzer - INFO - Выбросы: 8350 объектов
2026-03-26 13:08:16,474 - src.data_quality_analyzer - INFO - Рекомендаций: 1
2026-03-26 13:08:16,474 - src.data_quality_analyzer - INFO - Анализ качества источника: Source_B
2026-03-26 13:09:52,442 - src.data_quality_analyzer - INFO - Полнота: 56.4%
2026-03-26 13:09:52,443 - src.data_quality_analyzer - INFO - Топология: 100.0% валидных
2026-03-26 13:09:52,443 - src.data_quality_analyzer - INFO - Выбросы: 2931 объектов
2026-03-26 13:09:52,443 - src.data_quality_analyzer - INFO - Рекомендаций: 1
2026-03-26 13:09:52,444 - src.data_quality_analyzer - INFO - Отчёт о качестве данных: D:\mts\reports\data_quality_report.json
2026-03-26 13:09:52,444 - __main__ - INFO - 
Шаг 3/8: Очистка
2026-03-26 13:09:52,444 - src.geometry_cleaner - INFO - Очистка: 171454 записей (порог: 10.0 м²)
2026-03-26 13:09:52,465 - src.geometry_cleaner - INFO - Классификация сараев/гаражей...
2026-03-26 13:09:57,183 - src.geometry_cleaner - INFO - Найдено: 7671 (4.5%)
2026-03-26 13:09:57,183 - src.geometry_cleaner - INFO - Очистка геометрий...
2026-03-26 13:11:33,724 - src.geometry_cleaner - INFO - Удалено: 1389 (0.8%), осталось: 170065
2026-03-26 13:11:33,725 - src.geometry_cleaner - INFO - Очистка: 161076 записей (порог: 10.0 м²)
2026-03-26 13:11:33,751 - src.geometry_cleaner - INFO - Классификация сараев/гаражей...
2026-03-26 13:11:38,088 - src.geometry_cleaner - INFO - Найдено: 106 (0.1%)
2026-03-26 13:11:38,088 - src.geometry_cleaner - INFO - Очистка геометрий...
2026-03-26 13:13:09,311 - src.geometry_cleaner - INFO - Удалено: 269 (0.2%), осталось: 160807
2026-03-26 13:13:09,311 - __main__ - INFO - 
```

Как мы видим очистка удалила не совсем много данных, но это те данные, которые сильно испорчены. На этом этапе так же было выявлено, что многийе строки из разных датасетов имеются связи many-to-many или many-to-one, что усложняет задачу объединения.

#### Четвертый шаг "Объединение"
Это самый сложный пункт нашего модуля.
Логика модуля реализована в файле data_merge.py

Выбран был смешанный подход:
```
┌─────────────────────────────────────────────────┐
│  Этапы объединения в DataMerger                  │
├─────────────────────────────────────────────────┤
│  1. Пространственное сопоставление (2 этапа):    │
│     ├─ 1.1. Геометрическое пересечение (sjoin)   │
│     └─ 1.2. Поиск по центроидам (cKDTree)        │
│                                                  │
│  2. Разрешение конфликтов:                       │
│     └─ Многие-к-одному → выбор по максимальному  │
│        коэффициенту IoU                          │
│                                                  │
│  3. Слияние атрибутов:                           │
│     └─ Левое соединение с префиксами колонок     │
│                                                  │
│  4. Логика агрегации высоты:                     │
│     └─ Правило MAX из доступных источников       │
└─────────────────────────────────────────────────┘
```
## 1. SJoin
Функция sjoin из GeoPandas выполняет пространственное соединение двух гео-датафреймов на основе топологического предиката. В данном случае используется intersects — объекты считаются совпадающими, если их геометрии имеют хотя бы одну общую точку (пересекаются, касаются или один содержит другой).

Для каждой пары пересекающихся объектов вычисляется:

  -  IoU (Intersection over Union) — коэффициент качества совпадения.
  -  Расстояние между центроидами — дополнительная метрика близости.
  -  Пары с IoU ниже порога (MIN_IOU_THRESHOLD = 0.1) отбрасываются.

Преимущества sjoin:

  -  Использует оптимизированный пространственный индекс (R-tree) — поиск за O(log n) вместо O(n²).
  -  Нативная поддержка в GeoPandas — меньше кода, меньше багов.
  -  Автоматическая обработка сложных геометрий (MultiPolygon, отверстия).

## 1.1 cKDTree
```
coords_a = np.array([(g.centroid.x, g.centroid.y) for g in gdf_a.geometry])
tree_b = cKDTree(coords_b)
distances, indices = tree_b.query(coords_a, k=1)
```

Алгоритм поиска ближайшего соседа с использованием k-d дерева (k-dimensional tree) из библиотеки SciPy.

Как работает:
  -  Для каждого объекта вычисляется центроид (геометрический центр).
  -  Координаты центроидов источника Б преобразуются в k-d дерево — структуру данных, позволяющую выполнять поиск ближайшего соседа за логарифмическое время.
  -  Для каждого центроида источника А находится ближайший центроид в источнике Б.
  -  Если расстояние не превышает порог (MAX_DISTANCE_METERS = 50 м), пара считается потенциальным совпадением.

Почему используется как второй этап:
```
Сценарий: Здание из источника А не пересекается ни с одним объектом из Б,
          но находится рядом (погрешность оцифровки, разные системы координат).

Решение: Центроидный поиск "ловит" такие случаи, когда геометрии не пересекаются,
         но объекты явно представляют одно и то же здание.
```

## 1.2 IoU
Метрика качества пространственного совпадения двух полигонов.

Формула:
```
         Площадь пересечения
IoU = ─────────────────────────
      Площадь объединения
```
Интерпретация значений:
```
IoU = 1.0  → Идеальное совпадение (геометрии идентичны)
IoU = 0.5  → Половина площади общая, половина — разная
IoU = 0.1  → Минимальное допустимое перекрытие (порог из конфига)
IoU = 0.0  → Нет пересечения или ошибка вычисления
```

## 2. Разрешение many-to-one (many-to-many)
```
resolved = matches_df.loc[matches_df.groupby('id_a')['iou'].idxmax()].reset_index(drop=True)
```
Проблема: Одному объекту из источника А могут соответствовать несколько кандидатов из источника Б. И наоборот.

Пример:
```
Источник А: [Здание_123] (большой полигон)
Источник Б: [Гараж_456], [Дом_789], [Навес_012] (три маленьких полигона внутри Здание_123)

Результат sjoin: 3 совпадения с разными IoU
```

Стратегия разрешения: Для каждого id_a оставляем только одно соответствие — то, у которого максимальный IoU.

Почему максимальный IoU, а не минимальное расстояние:

  -  IoU учитывает и перекрытие, и размер — более комплексная метрика
  -  Минимальное расстояние может выбрать маленький объект рядом с большим, хотя они не связаны
  -  IoU естественным образом штрафует случаи "маленький внутри большого"

## 3. Слияние атрибутов: левое соединение с префиксами
```
merged = gdf_a.copy()  # За основа берётся источник А

# Добавляем информацию о сопоставлении
merged['matched_id_b'] = ...
merged['match_iou'] = ...
merged['match_type'] = ...

# Добавляем колонки из источника Б с префиксом "B_"
for col in gdf_b.columns:
    if col not in merged.columns and col != 'geometry':
        merged[f'B_{col}'] = merged['matched_id_b'].apply(
            lambda x: gdf_b_indexed.loc[x, col] if x in gdf_b_indexed.index else None
        )
```
Тип соединения: Левое соединение (left join) — сохраняются все записи из источника А, данные из Б добавляются только для найденных соответствий.

Почему именно левое соединение:

  -  Источник А считается основным (более полным или приоритетным)
  -  Здания без соответствия в Б не теряются — они просто остаются без дополнительных атрибутов
  -  Предсказуемое поведение: количество строк в результате = количество строк в источнике А

Обработка отсутствующих соответствий:
```
lambda x: gdf_b_indexed.loc[x, col] if x is not None and x in gdf_b_indexed.index else None
```

  -  Если для объекта из А не найдено соответствие в Б → значение остаётся None (NaN)
  -  Это корректно: отсутствие данных ≠ ошибка, модель должна уметь работать с пропусками

Логика агрегации высоты: правило MAX
```
def calculate_max_height(row):
    heights = []
    
    # 1. Прямое значение высоты из источника Б
    if pd.notna(row.get('B_height')) and row['B_height'] > 0:
        heights.append(row['B_height'])
    
    # 2. Расчётная высота: этажность × высота этажа
    if pd.notna(row.get('B_stairs')) and row['B_stairs'] > 0:
        floor_h = row.get('B_avg_floor_height', 3.0)
        heights.append(row['B_stairs'] * floor_h)
    
    # 3. Расчётная высота из источника А
    if pd.notna(row.get('gkh_floor_count_max')) and row['gkh_floor_count_max'] > 0:
        heights.append(row['gkh_floor_count_max'] * 3.0)
    
    return max(heights) if heights else None
```
Проблема: Высота здания может быть указана в разных источниках по-разному:

  -  Прямое измерение (лидар, кадастр).
  -  Расчёт через этажность и среднюю высоту этажа.
  -  Отсутствие данных в одном из источников.
Стратегия: Взять максимальное из всех доступных оценок.

### Пятый этап 
## Генерация признаков
```
FeatureEngineer
├── __init__()                          # Инициализация состояния
│
├── extract_all_features()              # Главный публичный метод (оркестратор)
│   ├── _add_geometry_features()        # Признаки формы и размера
│   ├── _add_spatial_features()         # Признаки положения в городе
│   ├── _add_neighborhood_features()    # Признаки окружения
│   ├── _add_categorical_features()     # Кодированные категории
│   ├── _add_statistical_features()     # Статистические нормализации
│   └── _add_address_features()         # Признаки адресной информации
│
├── _extract_building_type()            # Вспомогательный: классификация по тексту
├── _is_residential()                   # Вспомогательный: определение жилого типа
├── get_feature_columns()               # Получение списка сгенерированных признаков
└── export_feature_stats()              # Экспорт статистики по признакам
```

### Главный метод: extract_all_features
Метод последовательно вызывает приватные методы для каждой категории признаков. Порядок выбран осознанно:
  -  Геометрические признаки — базовые, не зависят от других вычислений
  -  Пространственные признаки — требуют только геометрию и координаты
  -  Признаки окружения — требуют доступ ко всему датасету (gdf_all)
  -  Категориальные признаки — могут зависеть от предварительно извлечённых текстовых полей
  -  Статистические признаки — вычисляются относительно распределения других признаков, поэтому идут после них
  -  Адресные признаки — простые бинарные индикаторы, могут добавляться в конце
### Метод _add_geometry_features: признаки формы и размера
Детальное описание признаков:
  -  feat_log_area - логарифм площади
  -  feat_perimeter - периметр
  -  feat_compactness - компактность формы
  -  feat_elongation - вытянутость
  -  feat_vertices_count - количество вершин полигона
  -  feat_convex_hull_area - площадь выпуклой оболочки
  -  feat_convex_hull_ratio - отношение площади к выпуклой оболочке

### Метод _add_spatial_features: признаки положения в городе
Детальное описание признаков
  -  feat_distance_to_center_km - расстояние до центра города
  -  feat_x_coord и feat_y_coord - нормированные координаты
  -  feat_h3_index - индекс геохеша системы H3

### Метод _add_neighborhood_features: признаки окружения
Алгоритм работы

Построение пространственного индекса: Для ускорения поиска соседей используется структура данных cKDTree из SciPy. Координаты центроидов всех объектов в gdf_all преобразуются в массив и передаются в конструктор дерева. Это позволяет выполнять поиск всех соседей в заданном радиусе за время O(log n) вместо O(n) при наивном переборе.

Итерация по радиусам: Метод перебирает список радиусов из конфигурации (NEIGHBOR_RADII_METERS, по умолчанию [25, 50, 100, 200] метров). Для каждого радиуса вычисляются четыре типа статистик:

  -  Количество соседей (feat_neighbor_count_{radius}m) — число объектов в заданном радиусе, исключая само здание.
  -  Cредняя площадь соседей (feat_avg_neighbor_area_{radius}m) — аналогично высоте, но для площади.
  -  Плотность застройки (feat_density_{radius}m) — количество соседей на гектар площади круга заданного радиуса.

### Метод _add_categorical_features: кодирование категориальных признаков
Пример работы:
```
Входной текст: "жилое здание, 5 этажей, кирпич"
Ключевые слова residential: ['жилое', 'жилой', 'residential', 'апартамент']
Результат: 'residential' (так как 'жилое' найдено в тексте)
```
Обработка неизвестных категорий: Если ни одно ключевое слово не найдено, возвращается значение 'other'. 
Это предотвращает потерю данных и позволяет модели учиться на остаточной категории.

Кодирование через LabelEncoder

Обучение на тренировочных данных: Если is_train=True, создаётся новый LabelEncoder, который обучается на уникальных значениях признака feat_building_type. Метод fit_transform преобразует строковые категории в целые числа (0, 1, 2, ...), сохраняя отображение внутри энкодера.
Применение на тестовых данных: Если is_train=False, метод ищет ранее сохранённый энкодер в словаре self.label_encoders. Если энкодер найден, применяется transform с обработкой неизвестных категорий: значения, не встречавшиеся при обучении, получают код -1.

Метод _is_residential проверяет наличие ключевых слов жилого типа в тегах или назначении здания.
Результат — бинарный признак feat_is_residential (1 для жилых, 0 для остальных).

Если в данных присутствует колонка district, она обрабатывается аналогично типу здания: извлечение → кодирование → обработка неизвестных значений. Если колонка отсутствует, признак получает значение 0, что эквивалентно «район неизвестен».

### Метод _add_statistical_features: статистические нормализации
  -  Z-score стандартизация - Z-score показывает, на сколько стандартных отклонений значение отклоняется от среднего. Например, z = 2.0 означает, что значение на два стандартных отклонения выше среднего.
  -  Квантильное ранжирование - quantile = rank(pct=True) — процентный ранг значения в распределении. Например, квантиль 0.95 означает, что значение больше 95% наблюдений.

### Метод _add_address_features: признаки адресной информации
Бинарные индикаторы:
  -  feat_has_address — равен 1, если поле gkh_address не пустое; 0 иначе.
  -  feat_has_number — аналогично для поля number (номер дома).

Композитный признак полноты:
  -  feat_address_completeness — среднее арифметическое двух бинарных признаков. Принимает значения 0.0 (нет ни адреса, ни номера), 0.5 (есть что-то одно) или 1.0 (есть и то, и другое).

# Примеры использования
```
from src.feature_engineering import FeatureEngineer
from config.config import config

# Подготовка данных (после очистки и слияния)
gdf_merged = ...  # GeoDataFrame с объединёнными данными

# Инициализация и генерация
fe = FeatureEngineer()
gdf_with_features = fe.extract_all_features(
    gdf_merged,
    is_train=True,           # Обучаем кодировщики
    gdf_all=gdf_merged       # Используем весь датасет для признаков окружения
)

# Получение матрицы признаков для модели
feature_cols = fe.get_feature_columns()
X = gdf_with_features[feature_cols]
y = gdf_with_features['final_height_m']  # Целевая переменная

# Экспорт статистики для аудита
fe.export_feature_stats(gdf_with_features, 'results/feature_stats_train.csv')
```
# Сценарий инференса: генерация признаков для новых данных
```
# Предположим, модель уже обучена, и есть сохранённый FeatureEngineer
# (на практике инженер признаков нужно сериализовать вместе с моделью)

fe = load_feature_engineer('models/feature_engineer.pkl')  # Псевдокод

# Новые данные для предсказания
gdf_new = ...  # GeoDataFrame с новыми зданиями

# Генерация признаков в тестовом режиме
gdf_pred = fe.extract_all_features(
    gdf_new,
    is_train=False,          # Используем сохранённые кодировщики
    gdf_all=gdf_new          # Для признаков окружения используем только новые данные
)

# Предсказание
X_pred = gdf_pred[fe.get_feature_columns()]
predictions = model.predict(X_pred)
```
# Отладка: проверка сгенерированных признаков
```
# Быстрый просмотр статистики по признакам
import pandas as pd

fe = FeatureEngineer()
gdf_feat = fe.extract_all_features(gdf_sample, is_train=True, gdf_all=gdf_sample)

# Выборка числовых признаков для анализа
numeric_feats = [c for c in gdf_feat.columns if c.startswith('feat_') 
                 and gdf_feat[c].dtype in ['float64', 'int64']]

print(gdf_feat[numeric_feats].describe().T[['mean', 'std', 'min', 'max', 'count']])

# Проверка на пропуски
null_summary = gdf_feat[numeric_feats].isna().sum().sort_values(ascending=False)
print("Пропуски в признаках:")
print(null_summary[null_summary > 0])
```
# Challenge IT CUP 26 | MTS TRUE TECH.
# Команда Пока без названия.
Ссылка на Google Disk: https://drive.google.com/drive/folders/1KjRhedxerQenK1aI5kppFEcr-Ygyuk6m?usp=drive_link
Веса, датасеты, графики и др. файлы, которые по некоторым причинам не поместились в репозиторий лежат гугл диске.
